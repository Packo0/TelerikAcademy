//Write an expression that calculates rectangle`s area by given width and height
var area,
    width = 2.5,
    height = 3;

area = width * height;
console.log("The area of rectangle with width = " + width + " and height = " + height + " is: " + area);
